---
name: pd
description: 面向中国团队的产品经理技能，支持需求发现、竞品研究、需求池与优先级、PRD、验收和上线复盘，并按需交付飞书文档、原型和汇报材料。Use for product discovery, prioritization, PRDs, release planning, and product reviews for Chinese-market products.
---

# 产品经理总控技能

把需求推进为有证据、有取舍、可评审和可验收的产品交付物。默认使用简体中文，首次出现的英文术语给出中文解释；用户指定其他语言时遵从用户。文件名和标识符使用英文。

## 先选择交付范围

先读现有需求、PRD、反馈和决策记录，复用已确认信息。只问会改变目标用户、业务边界、交付范围或发布位置的问题；普通缺口标记为假设并继续。

| 用户任务 | 执行方式 | 交付边界 |
| --- | --- | --- |
| 单功能、小改版、快速评审 | 轻量模式 `lean` | 一篇功能 PRD，附必要证据、需求池和验证计划 |
| 新产品、跨角色系统、完整 PRD | 标准模式 `standard`（脚本默认） | 分析材料 + `00–18` 共 19 篇 PRD |
| 仅访谈整理、竞品分析、需求排序、PRD 评审、复盘 | 专项任务 | 直接完成所需产物，不初始化整套文档 |
| 原型、PPT、运营素材、飞书发布 | 按用户范围追加 | 区分 brief、可预览代码、真实文件及已发布结果 |

范围未指定时，对新产品采用标准模式，对明确的局部改动采用轻量模式，并说明选择。用户已有完整 PRD 时直接进入指定阶段。不要因模板存在就追加无关产物、替换业务或扩大外部操作范围。

## 按需读取

以下路径相对于本 Skill 根目录；从业务项目运行脚本时使用脚本绝对路径，输出目录相对于业务项目。

| 当前任务 | 读取资料 |
| --- | --- |
| 国内业务、角色、渠道、协作 | [中国场景指南](references/china-pm-playbook.md) |
| 访谈、竞品、证据、机会验证 | [发现与证据](references/discovery-and-evidence.md) |
| 需求排序、商业判断、路线图 | [方法选择与计算口径](references/product-thinking-frameworks.md) |
| PRD 编写、评审、验收、上线复盘 | [交付质量标准](references/prd-quality-gates.md) |
| 初始化或确认产物 | [目录与模式](references/artifact-structure.md) |
| 飞书发布 | [发布流程](references/feishu-publishing.md)，涉及复杂图表再读[画板流程](references/feishu-whiteboard.md) |
| 原型实现 | `subskills/frontend-design/SKILL.md` 和 [UI 参考](references/prototype-ui-benchmarks.md) |
| 来源核查与更新 | [方法来源与本地化说明](references/pm-source-map.md)、[子技能来源](references/vendor-sources.md) |

## 贯穿全程的约定

1. **先问题后方案。** 区分用户原话、观察事实、产品判断和待验证假设。老板或大客户提出的功能也要明确问题和机会成本。
2. **结论能回查。** 外部事实附来源和访问日期，内部材料附位置与统计口径。没有访谈、数据或工具访问结果，就不能声称已经验证。
3. **判断服务决策。** 选适用的方法，不堆框架、不虚构分数，不把社区经验或单个案例写成全行业标准。
4. **持续推进当前范围。** 不在分析、初稿或 brief 完成后无故停下。阻塞只影响依赖它的部分；继续其他本地产物。
5. **不替人做承诺。** 估算、建议负责人、建议日期、合同约束和已确认承诺分别标记。生成排期不代表研发承诺，草案不等于评审通过。
6. **发布按授权。** 用户已要求发布且目标可唯一定位时继续执行，不重复询问。仅有“准备发布”时生成计划；未要求发布时完成本地交付即可。
7. **保护已有人工作业。** 初始化只补缺失文件，不覆盖正文或发布清单。改需求时同步规则、验收、指标和决策记录。

## 工作流

### 1. 明确上下文并建档

记录目标用户、使用者/决策者/付费者、核心场景、业务目标、成功口径、现有替代办法、资源约束、交付模式。已知信息直接继承。

在业务项目目录运行（将 `/path/to/pd-skill` 替换为实际安装路径）：

```bash
python3 /path/to/pd-skill/scripts/init_pm_case.py --title "需求标题"
python3 /path/to/pd-skill/scripts/init_pm_case.py --title "单功能迭代" --profile lean --nested
```

默认写入 `.pd/`；`--nested` 写入 `.pd/<slug>/`；多项目用独立目录。脚本只生成骨架，不代表内容已完成。填写 `brief/00-request.md` 后按所选模式继续。

### 2. 发现问题与收集证据

阅读发现与证据指南。优先用已有访谈、工单、数据，再针对决策缺口检索官方资料和社区信号。新方向通常需要市场/竞品、用户痛点、行业变化、技术约束；局部改动只调查有关问题，不强制凑数量。

- `brief/01-web-research.md`：检索问题、竞品与替代方案、来源、反证、对决策的影响。
- `analysis/03-evidence-ledger.md`：用 `E-001` 等编号记录事实、推断、假设及来源；访谈与公开评论不等同于统计代表性。
- 无法联网时写明限制，继续用已有材料生成带假设草案，不标记为“全网调研完成”。不依赖特定的 `WebSearch`/`WebFetch` 工具名。

### 3. 方案发散、收敛与验证

需要方案探索时读取 `subskills/brainstorming/SKILL.md` 的方法。使用其问题拆解和方案比较方法；执行范围、提问节奏和授权以用户指令为准，不额外套逐阶段批准流程。

`analysis/01-brainstorm.md` 写清问题、目标/非目标、方案与替代办法、推荐理由、关键假设、为何现在处理。已有明确方案的小改动只比较真实存在的取舍。

标准模式在 `analysis/02-jobs-product-lens.md` 保留聚焦判断：一句话价值、首要任务、不做清单、端到端体验和可砍内容。这些问题可独立完成，不要求安装人格类 Skill。可用简短发布说明表达价值，不能编造用户引语。

对高影响、低证据的假设，在 `analysis/05-validation-plan.md` 定义验证方法、成功/失败条件、负责人和下一步。可用访谈、可用性测试、人工试点或实验；尚未执行就标为计划。

### 4. 需求池与优先级

维护 `analysis/04-demand-backlog.md`：`REQ-ID`、来源证据、用户任务、价值、优先级及依据、成本/依赖、决策人、状态、目标版本。同一问题合并并保留各来源；紧急程度、缺陷严重度和开发优先级分开。

先识别已确认的合同/上线阻塞等硬约束，再比较可选需求。数据充分可用 RICE（触达×影响×信心÷投入）；不足时先用价值/投入及证据说明。不能用编造分数掩盖未知。最终取舍与不做理由写入 PRD 范围页。

国内 ToB 区分标准能力、可配置能力、项目定制，核算实施与维护成本；ToC 关注实际完成核心任务的人群、转化、留存及渠道差异。

### 5. 编写并复审 PRD

阅读 `subskills/deliver-prd/SKILL.md` 与交付质量标准，必要时读取其模板。轻量模式把相同内容收进 `prd/01-feature-spec.md`；标准模式保持 `00–18` 的稳定文件名。

| 交付主题 | 标准模式落点 | 要回答的问题 |
| --- | --- | --- |
| 定位与问题 | `00–04` | 为什么做、证据是什么、为谁做、成功怎么算 |
| 方案与实现边界 | `05–10`、`13` | 主异常流程、需求、规则、权限、接口边界、页面状态 |
| 范围与决策 | `11–12`、`16–17` | 做什么/不做什么、依赖、风险、负责人、变更影响 |
| 数据与经营闭环 | `14–15` | 埋点口径、验收门槛、灰度回退、观察窗口、复盘决策 |
| 来源与素材 | `18` | 可分享的参考资料和术语 |

核心需求建立 `E-ID → REQ-ID → BR-ID → AC-ID → 指标/版本` 追踪关系，不必一对一。业务规则明确权限、状态迁移、边界和重复/失败处理。验收用 Given/When/Then（前提/动作/结果）写成可观察结果。

技术章节区分已有事实、产品约束和待研发确认的建议，不把模板架构当既定选型。数据章节明确分母、去重、时区、时间窗和排除条件。未知基线标为待采集。

图表按表达需要选用 Mermaid 或表格；需要画图时读 `subskills/utility-mermaid-diagrams/SKILL.md`。标准模式通常提供用户流程、功能结构和系统边界图；简单变更可用规则表，不设装饰性配额。保留源文本，飞书渲染与可编辑性须在实际发布后验证。

### 6. 验收、上线与复盘

在 `08/15/16/17` 或轻量 PRD 对应章节区分可评审、可供研发估算、可上线放量三个状态。列明当前证据与未决事项。

上线计划要有放量人群、观察窗口、护栏指标、暂停/回退触发条件和责任角色。复盘比较目标、实际、成本与异常，决定扩大、迭代、回退或停止。没有上线数据时只交复盘计划，不生成“已达成”的结果。

### 7. 完成已要求的附加交付

- **飞书**：按发布指南检查登录与权限，唯一解析已授权目标，再按 `manifest.json` 创建/更新文档。默认只发布 `prd/`；访谈、证据和需求池不随正文外传。保存逐篇链接与失败状态，部分成功不等于发布完成。
- **原型**：读取前端子技能；根据本项目 PRD 生成 brief、页面流、覆盖清单，再交付可预览代码和启动方式。用户只要 brief 时以 brief 为边界。角色与页面由需求决定，不套固定行业页面。
- **PPT**：读取 `subskills/utility-slideshow-creator/SKILL.md`，输出逐页内容与规格。生成并核对 `.pptx` 后才称为 PPT 文件；大纲不能冒充成品。
- **运营素材**：按渠道输出受众、卖点、文案、规格和实验变体。实际出图并校验后才称为图片交付。

## 完成检查与汇报

按本次范围检查，不以文件数量判定完成：

- 结论区分事实/推断/假设，有证据或明确缺口；不存在虚构访谈、数据和审批。
- 目标、优先级、方案及范围一致；核心需求可追踪到规则、验收和指标。
- 关键异常、权限与数据边界可测试；未决事项明确责任角色、影响和建议解决时间。
- 上线与复盘条件可执行；“待补充”骨架不冒充可开发 PRD。
- 已要求的原型/文件/发布有可检查产物；遇到阻塞报告已完成部分与具体缺口。

最终说明交付模式、路径、重要取舍、验证结果及剩余待定项。文档质量通过不代表商业假设验证、业务审批或实际上线已完成。
